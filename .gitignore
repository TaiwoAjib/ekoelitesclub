# Secrets — never commit
.env

# OS / editor noise
.DS_Store
Thumbs.db

# Node (if dependencies are ever added)
node_modules/

# Uploaded photos — the durable copy lives in the database (images table);
# these on-disk files are just a regenerable cache.
public/images/executives/
public/images/events/
